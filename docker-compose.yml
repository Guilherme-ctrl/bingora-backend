services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: bingo
      POSTGRES_PASSWORD: bingo
      POSTGRES_DB: bingo
    volumes:
      - bingo_pg_data:/var/lib/postgresql/data

volumes:
  bingo_pg_data:
